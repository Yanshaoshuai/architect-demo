<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yan.architect.architectdemo.mapper.StudentMapper">

    <resultMap type="com.yan.architect.architectdemo.pojo.Student" id="StudentMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="age" column="age" jdbcType="INTEGER"/>
        <result property="sex" column="sex" jdbcType="INTEGER"/>
        <result property="address" column="address" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="selectStudentVo">
        select id, name, age, sex, address, create_time, update_time from student
    </sql>

    <!--查总数 -->
    <select id="getCount" resultType="Integer">
        select count(0) from student
    </select>

    <!--查询单个-->
    <select id="queryById" resultMap="StudentMap">
        <include refid="selectStudentVo"/>
        where id = #{id}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" parameterType="com.yan.architect.architectdemo.pojo.Student" resultMap="StudentMap">
        <include refid="selectStudentVo"/>
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="age != null">
                and age = #{age}
            </if>
            <if test="sex != null">
                and sex = #{sex}
            </if>
            <if test="address != null and address != ''">
                and address = #{address}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" parameterType="com.yan.architect.architectdemo.pojo.Student" keyProperty="id"
            useGeneratedKeys="true">
        insert into student(name, age, sex, address, create_time, update_time)
        values (#{name}, #{age}, #{sex}, #{address}, #{createTime}, #{updateTime})
    </insert>

    <!--通过主键修改数据-->
    <update id="update" parameterType="com.yan.architect.architectdemo.pojo.Student">
        update student
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="age != null">
                age = #{age},
            </if>
            <if test="sex != null">
                sex = #{sex},
            </if>
            <if test="address != null and address != ''">
                address = #{address},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from student where id = #{id}
    </delete>

</mapper>